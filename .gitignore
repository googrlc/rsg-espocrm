.DS_Store
*.env
.env
.venv/
.venv-*/

# Local clone for side-by-side compare (see espocrm-compare.code-workspace)
official-espocrm/

# Never nest a second copy of this repo here (causes duplicate trees and confusion)
rsg-espocrm/
