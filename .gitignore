.DS_Store
*.env
.env
.venv/
.venv-*/

# Optional local vanilla Espo clone for diffs (never commit upstream sources)
official-espocrm/

# Never nest a second copy of this repo here (causes duplicate trees and confusion)
rsg-espocrm/

# Local clone of canonical schema repo (push DDL to googrlc/rsg-data-schema when permitted)
rsg-data-schema/
